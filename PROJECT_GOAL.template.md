# Project Goal Template

Copy this file to `PROJECT_GOAL.md` for the no-prompt agent path.

`PROJECT_GOAL.md` is local starter input. The durable roadmap and slice packet
become the real repo authority after the agent creates or updates them.

## Goal

<describe what I want to build>

Replace the placeholder above with one or two paragraphs. Include the audience,
the core thing the repo should do, and any important constraints.

## Optional First Slice Preference

<describe the smallest first piece of work, or delete this line>

## First Slice

Create or update the roadmap and first slice packet before coding. Do not code
until the packet validates.

## Agent Rule

If `PROJECT_GOAL.md` still contains placeholder text, ask exactly:

```text
What do you want to build? One or two paragraphs is enough.
```

Do not create or update a roadmap, packet, or code until the goal is known.
